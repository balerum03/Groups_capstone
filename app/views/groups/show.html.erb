<p id="notice"><%= notice %></p>

<p>
  <%= image_tag @group.image, :class => 'col-1 rounded-circle' %>
  <%= @group.name %>
  <%= @group.created_at %>

  <% @group.items.each do |i| %>
    </br>
    <tr>
      <td><%= i.name %></td>
      <td><%= i.created_at %></td>
    </tr>
  <% end %>
</p>
